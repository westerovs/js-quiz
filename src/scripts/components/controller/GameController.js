export default class GameController {
  constructor(deckModel, deckView) {
    this.deckModel = deckModel;
    this.deckView = deckView;
  }

  startGame() {
    // Логика начала игры
  }

  flipCard() {
    // Логика переворота карточки
  }

  swipeRight() {
    // Логика свайпа вправо
  }
}
