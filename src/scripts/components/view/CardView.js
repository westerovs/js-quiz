export default class CardView {
  constructor(cardModel) {
    this.cardModel = cardModel;
  }

  render() {
    // Логика отображения карточки
  }

  flip() {
    // Логика анимации переворота карточки
  }
}
