export default class DeckView {
  constructor(deckModel) {
    this.deckModel = deckModel;
    this.cardViews = [];
  }

  render() {
    // Логика отображения колоды
  }

  update() {
    // Логика обновления представления при изменениях в модели
  }
}
